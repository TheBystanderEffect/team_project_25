export class Diagram {
    get layers() {
        return this._layers;
    }
    set layers(layers) {
        this._layers = layers;
    }
    addLayer(layer) {
        this._layers.push(layer);
    }
}
//# sourceMappingURL=Diagram.js.map